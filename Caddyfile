:{$PORT:80} {
    root * /app/public
    encode zstd gzip

    # Send Laravel API requests to PHP.
    handle /api* {
        php_server
    }

    # Serve React assets and fall back to the SPA entry point.
    handle {
        try_files {path} /index.html
        file_server
    }
}
